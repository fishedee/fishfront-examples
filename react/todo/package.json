{
  "name": "counter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
  },
  "scripts": {
    "start": "node build/server.js",
    "babel": "babel --version && babel src --out-dir build --watch",
    "watch": "node src/server-bridge",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
